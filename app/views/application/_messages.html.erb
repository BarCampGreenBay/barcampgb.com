<% flash.each do |type, message| %>
  <div class="alert alert-<%= type %> alert-block fade in flash-message">
    <a class="close" data-dismiss="alert" href="#">&times;</a>
    <p><%= message %></p>
  </div>
<% end %>